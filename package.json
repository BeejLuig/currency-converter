{
  "name": "currency-converter",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": ". ./setup.sh && node server.js",
    "start:prod": ". ./setup.sh prod && node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "express": "^4.16.3"
  },
  "devDependencies": {
    "node-mocks-http": "^1.7.0"
  }
}
